This folder is for all documents and submissions related to the implementation phase I.

GUI options of the Configuration file:
list country (calculate on every click the remaining country choices)
edit telesales url
edit customer number (on input checks if it consists only from numbers)
edit tariff
edit tariff campaign control
table
	edit addon id
	edit addon addon
edit domain (adds "{TIMESTAMP}" before the last dot)
list domain-bundle (false or true)